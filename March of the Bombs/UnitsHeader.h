#pragma once

class Units
{
private:
	void makeUnit();			//calls upon the unit factory to make a unit
	void performUnitAction();	//will probably join this with move unit
	void moveUnit();			//will probably join this with unit action
};